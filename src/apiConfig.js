// src/apiConfig.js

const API_URL = ""; // Reemplaza con tu URL base de la API

export const config = {
  API_URL,
  headers: {
    "Content-Type": "application/json",
    // Otros encabezados comunes, como token de autenticación
  },
};
